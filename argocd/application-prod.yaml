apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-deploy-demo-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Ritesh205/k8s-deploy-demo.git
    targetRevision: deploy                    # 👈 use deploy branch for values
    path: helm/k8s-deploy-demo
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
