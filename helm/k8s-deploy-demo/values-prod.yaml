# Production environment values
environment: prod
replicaCount: 1

image:
  tag: "sha-2683f9c8d061bc7812b627d65fff1d679c0ba3bd"

service:
  nodePort: 30082

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

springProfile: prod

configMap:
  LOG_LEVEL: WARN

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 1
  targetCPUUtilizationPercentage: 80
